/*
   Starting from the markup of the version done in plain JavaScript, 
   remake the slider, but this time using Vue.
*/

//--- Preparation phase
//? Preapre HTML structure
//JS:
// Destructuring createApp from vue object
// Create an instace of vue app
// Create an variable and active index with initial value
// Define functions increment and decremetn active index

//---Output phase
// Mount in DOM element

/* BONUS
    0 - Now, let's also create thumbnails! 
    1 - When you click on a thumbnail, display the corresponding image in large size.
    2 - Apply autoplay to the slider: every 3 seconds, automatically change the image. 
    3 - When the mouse hovers over the slider, stop the autoplay and resume it when it leaves.
*/

// ---Bonus 0
// Prepare HTML CSS structure
// In thumbnails img element create v-for img in media

//--- Bonus 1
// Create a function to do index === activeIndex
